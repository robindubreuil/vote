vote (1.4.2-1) unstable; urgency=medium

  * Fix: reveal button relabels to "Corriger les réponses" after first reveal so re-reveal reads as a distinct action (verified live that re-reveal already re-applies scores correctly; the static label made it feel like a no-op)

 -- Robin <63008968+robindubreuil@users.noreply.github.com>  Fri, 31 Jul 2026 23:30:51 +0200

vote (1.4.1-1) unstable; urgency=medium

  * Fix: gap restored between multiple-choice vote options and "Valider mon vote" (fieldset margin reset clobbered the grid's bottom margin)
  * Fix: empty top bar no longer shown on stagiaire join screen (header `hidden` attribute was overridden by `display: flex`)
  * Fix: footer showed "unknown" commit in Docker/.deb builds (.git absent → gen-version.js fallback); values now injected via CI build-args

 -- Robin <63008968+robindubreuil@users.noreply.github.com>  Fri, 31 Jul 2026 22:55:28 +0200

vote (1.4.0-1) unstable; urgency=medium

  * Security: Dashboard cookie Secure flag honoured behind TLS-terminating proxy (S17)
  * Security: Constant-time dashboard password compare via SHA-256 digests (S18)
  * Fix: showError surfaces mid-session errors via toast fallback (F29)
  * Fix: Reconnect backoff reaches ~16h instead of ~18min (F30)
  * Fix: Confirm-dialog Escape no longer leaks the in-flight promise (F31)
  * Fix: saveHighScore/setLastConfig surface quota failures instead of silent loss (F32)
  * Fix: Persistence history recovery on oversized lines (R23)
  * Fix: writePump exits promptly on disconnect via done channel (R24)
  * Fix: Client identity read via atomic.Pointer, eliminating data race (R25)
  * Fix: cleanupLoop holds RLock across reap, closing TOCTOU window (R26)
  * Tests: 621 frontend tests, full -race backend suite green

 -- Robin <63008968+robindubreuil@users.noreply.github.com>  Thu, 31 Jul 2026 12:20:00 +0100

vote (1.3.1-1) unstable; urgency=medium

  * Fix: RevealAnswers double-scoring on re-click (idempotency via LastVoteScores)
  * Fix: Reconnecting trainee stuck in WAITING when vote is CLOSED
  * Fix: Unify leaveSession paths — Esc shortcut now resets all competitive state
  * Fix: Trainee Total score uses server game score instead of local high score
  * Fix: Blank votes now score 0 instead of -500 in competitive mode
  * Security: Add c.Type trainer guards on all trainer-only handlers
  * Security: Validate reveal_answers colors against session active palette
  * Security: Clamp report_game_score to [0, 100000], require session.GameEnabled
  * Fix: Ranking now includes game scores (TotalScore = vote + game)
  * Tests: Add competitive mode, reveal, scoring, blank vote, game score tests
  * Docs: Sync CLAUDE.md with competitive mode, v3 presets, root hash URL
  * Packaging: Align /var/lib/vote perms (0700), bump nodejs dependency to 22

 -- Robin <63008968+robindubreuil@users.noreply.github.com>  Sat, 28 Jun 2026 20:35:00 +0100

vote (1.3.0-1) unstable; urgency=medium

  * Add competitive mode: server-side scoring, reveal UI, per-vote scoreboard
  * Add allow-blank-vote toggle (pseudo-color "blank" in vote array)
  * Add classroom leaderboard display (live top-3 via BroadcastChannel)
  * Add game score reporting from stagiaire mini-game
  * Improve mini-game: adaptive difficulty (4-8 colors by high score)
  * Improve mini-game: streak multiplier (1 + streak*0.25, capped at 3x)
  * Improve mini-game: gold pegs, peg animations, score count-up
  * Improve mini-game: level-up celebration banner, progress bar
  * Shorten stagiaire join URL to root hash form (#ABC via Caddy redirect)
  * Add presets schema v3 (competitive, allowBlank, correctColors)
  * Fix button spacing in stagiaire (name edit, blank vote)
  * Fix timer clock-skew (already in 1.2.1, formalized)

 -- Robin <63008968+robindubreuil@users.noreply.github.com>  Sat, 28 Jun 2026 19:30:00 +0100

vote (1.2.1-1) unstable; urgency=medium

  * Fix timer clock-skew: send elapsed seconds instead of absolute timestamp
  * Fix timer clamp to avoid negative display values

 -- Robin <63008968+robindubreuil@users.noreply.github.com>  Sat, 28 Jun 2026 16:00:00 +0100

vote (1.2.0-1) unstable; urgency=medium

  * Add maintainer dashboard with HMAC cookie auth (/dashboard)
  * Add server-side stats persistence (counters.json + stats.jsonl)
  * Add rate limiting to dashboard login (brute-force protection)
  * Fix graceful shutdown: os.Exit replaced with errCh pattern
  * Fix hub: VoteManager errors now propagated to client
  * Fix client: slow clients disconnected (SendJSON closes conn)
  * Fix store: corruption logged, rotation failure recovery
  * Fix frontend: Safari private-mode crash (storage try/catch)
  * Fix frontend: vote button double-fire removed
  * Fix frontend: timer leak on leaveSession
  * Fix tests: E2E session code format, rate limiter assertion
  * Security: CSP/X-Frame-Options/nosniff headers on dashboard
  * Security: /health returns 503 during drain
  * Cleanup: dead code removal, constant extraction, version alignment
  * Set VOTE_DATA_DIR=/var/lib/vote in default config
  * Remove unused /var/log/vote directory
  * Bump TimeoutStopSec to 15s

 -- Robin <63008968+robindubreuil@users.noreply.github.com>  Sat, 28 Jun 2026 10:00:00 +0100

vote (1.0.0-1) unstable; urgency=medium

  * Initial Debian packaging
  * WebSocket-based real-time voting system
  * Formateur and Stagiaire frontends included

 -- Robin <63008968+robindubreuil@users.noreply.github.com>  Thu, 08 Jan 2026 10:00:00 +0100
